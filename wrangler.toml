name = "ohouse-3d-asset-library"
main = "worker.js"
compatibility_date = "2025-01-01"

# Repo root is served as static assets. The Worker below just hands
# every request to the asset binding (no custom routing logic).
[assets]
directory = "."
binding = "ASSETS"
