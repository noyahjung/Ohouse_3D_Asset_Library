.claude/
.DS_Store
*.log

# npm artifacts from one-off tooling (e.g. scripts/bake-thumbs.mjs).
# The script itself lives in scripts/ and is committed; the install
# bits are throwaway.
node_modules/
package.json
package-lock.json
